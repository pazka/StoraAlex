# Security: do NOT run package lifecycle scripts on install (blocks malicious postinstall).
# Rely on prebuilt binaries (better-sqlite3, sharp). If a dep truly needs a build step,
# flag it to the owner instead of enabling scripts globally. See AGENTS.md.
ignore-scripts=true

# Reproducible, no surprise upgrades.
save-exact=true
fund=false
audit=true
